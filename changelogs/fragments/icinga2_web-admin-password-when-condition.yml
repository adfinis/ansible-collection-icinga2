---
bugfixes:
  - icinga2_web - fix the ``when`` condition of "Insert icingaweb2 admin
    password into database", which errored out on fresh installations because
    the registered result of the password hash task has no ``skipped`` key
    when the task actually ran.
